class Manager {
    public name() {
        
        // after set up what to do?

        // roll to determine who goes first
        // draw 4 cards

        // main phase
        // display cards to player
        // valid events: pick card or sell cards or end turn 
        // - pick card: hero upgrades or main phase action

        // offensive phase
        // up to 3 roll attempts
        // valid events select, reroll, choose valid offensive ability, roll phase action cards (either player), or end

        // defensive phase if offensive ability picked
        // valid events
        // roll (single roll attempt) or roll phase action cards (either player)

        // main phase 2
        // valid events
        // pick card or sell card or end turn

        // discard phase
        // valid events
        // sell card or end (if 6 or less cards in hand)


        // upkeep phase
        // no valid events (just display upkeep changes)

        // income phase
        // +1 cp and +1 card for player










    }
}